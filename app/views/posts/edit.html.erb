<h3>Edit</h3>

<%= form_for @post, url: {action: "update" } do |p| %>
  <%= p.label :title %>
  <%= p.text_field :title %>
  
  <%= p.label :body %>
  <%= p.text_area :body %>
  
  <%= p.submit "Submit" %>
<% end %>